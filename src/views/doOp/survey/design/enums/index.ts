// 组件类型
export enum ComponentType {
  Input = 'input',
  Radio = 'radio',
  Checkbox = 'checkbox',
}

export enum ModuleType {
  Module = 'module',
}

// input类型输入框大小
export enum InputSize {
  Small = 'small',
  Middle = 'middle',
  Big = 'big',
}
